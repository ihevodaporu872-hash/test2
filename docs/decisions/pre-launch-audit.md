# Предпусковой аудит — INTERIOR STUDIO

**Дата проведения:** 7 февраля 2026 г.

---

## 1. Проверка текстового контента (русский язык)

### Найденные проблемы:

| Файл | Проблема | Статус |
|---|---|---|
| `src/app/manifest.ts` | Описание на английском: «Premium interior design studio creating bespoke living spaces» | Исправлено |
| `src/components/sections/lead-form.tsx` | Плейсхолдер email на английском: `your@email.com` | Исправлено |

### Проверенные страницы (все на русском языке):
- Главная (`/`) — OK
- Портфолио (`/portfolio`) — OK
- Услуги (`/services`) — OK
- Пакеты и цены (`/pricing`) — OK
- Процесс работы (`/process`) — OK
- О нас (`/about`) — OK
- Контакты (`/contact`) — OK
- FAQ (`/faq`) — OK
- B2B (`/b2b`, `/b2b/mop`, `/b2b/parking`, `/b2b/planning-audit`) — OK
- Журнал (`/journal`) — OK
- Политика конфиденциальности (`/privacy`) — Заполнено в рамках аудита
- Пользовательское соглашение (`/terms`) — Заполнено в рамках аудита
- Тесты (`src/__tests__/`) — Английский допустим (тестовый код)

**Результат:** Весь пользовательский контент на русском языке.

---

## 2. Проверка изображений

### Файлы в `/public/images/`:
- `photo_main.jpg` — Главная, герой-секция
- `photo_1.jpg` ... `photo_10.jpg` — Портфолио, B2B, разделы

### Проверка ссылок на изображения в коде:
| Страница | Изображение | Статус |
|---|---|---|
| Главная (Hero) | `/images/photo_main.jpg` | Файл существует |
| О нас | `/images/photo_3.jpg` | Файл существует |
| Услуги | `/images/photo_2.jpg` | Файл существует |
| Портфолио | `/images/photo_5.jpg` | Файл существует |
| Стоимость | `/images/photo_4.jpg` | Файл существует |
| Процесс | `/images/photo_6.jpg` | Файл существует |
| B2B | `/images/photo_8.jpg` | Файл существует |
| B2B МОП | `/images/photo_7.jpg` | Файл существует |
| B2B Паркинг | `/images/photo_9.jpg` | Файл существует |
| B2B Планировочный аудит | `/images/photo_10.jpg` | Файл существует |
| OG Image | `/og-image.jpg` | Указан в конфиге (требует проверки наличия) |

**Примечание:** Файл `/og-image.jpg` указан в `siteConfig.ogImage`. Убедитесь, что он существует в `/public/` перед запуском.

**Результат:** Все ссылки на изображения в страницах корректны, файлы существуют.

---

## 3. Мобильная навигация

### Проверенные элементы:
- **Гамбургер-меню:** Используется Sheet (shadcn/ui) на экранах < `lg` — OK
- **Навигационные ссылки:** 7 пунктов (Главная, Портфолио, Услуги, Пакеты, Процесс, О нас, Контакты) — OK
- **CTA-кнопка:** «Консультация» в мобильном меню — OK
- **Закрытие меню:** SheetClose при нажатии на ссылку — OK
- **Анимация:** Последовательная анимация элементов при открытии — OK
- **Accessibility:** `aria-label="Открыть меню"`, `aria-label="Мобильная навигация"` — OK

**Результат:** Мобильная навигация работает корректно.

---

## 4. Валидация форм

### Форма заявки (LeadForm):
- **Обязательные поля:** Имя, Телефон — отмечены звёздочкой — OK
- **Zod-валидация:** `leadFormSchema` с клиентской и серверной проверкой — OK
- **Сообщения об ошибках:** На русском языке, привязаны к полям через `aria-describedby` — OK
- **Anti-spam:** Honeypot-поле, проверка времени заполнения, rate limiting по IP — OK
- **Состояния формы:** idle / submitting / success / error — все обрабатываются — OK
- **Accessibility:** `aria-invalid`, `role="alert"`, `aria-describedby` — OK

### Форма подписки (Footer):
- Базовый email-ввод, `onSubmit` с `preventDefault()` (без backend-обработки) — OK (информативно)

**Результат:** Формы имеют надёжную валидацию.

---

## 5. SEO и OG-метаданные

### Проверка по страницам:

| Страница | title | description | OG | canonical |
|---|---|---|---|---|
| `/` | OK | OK | OK | OK |
| `/portfolio` | OK | OK | OK | OK |
| `/services` | OK | OK | OK | OK |
| `/pricing` | OK | OK | OK | OK |
| `/process` | OK | OK | OK | OK |
| `/about` | OK | OK | OK | OK |
| `/contact` | OK | OK | OK | OK |
| `/faq` | OK | OK | OK | OK |
| `/journal` | OK | OK | Нет OG | Нет canonical |
| `/b2b` | OK | OK | OK | OK |
| `/b2b/mop` | OK | OK | OK | OK |
| `/b2b/parking` | OK | OK | OK | OK |
| `/b2b/planning-audit` | OK | OK | OK | OK |
| `/privacy` | OK | OK | Нет OG (допустимо) | OK |
| `/terms` | OK | OK | Нет OG (допустимо) | OK |

### Дополнительно:
- **JSON-LD (Schema.org):** Organization, Breadcrumb, FAQ, Service — OK
- **sitemap.xml:** Генерируется динамически, включает все страницы — OK
- **robots.txt:** Разрешает индексацию, указывает sitemap — OK
- **manifest.json:** Настроен для PWA — OK (описание исправлено на русский)
- **lang="ru"** в `<html>` — OK

### Замечания:
- `/journal` — страница-заглушка без OG и canonical. Рекомендуется добавить перед наполнением контентом.
- Страницы `/privacy` и `/terms` не имеют OG-изображений, что допустимо для юридических страниц.

**Результат:** SEO-метаданные настроены на всех основных страницах.

---

## 6. Проверка кода на ошибки

### Потенциальные проблемы:
- **console.log в submit-lead.ts:** Логирование заявок в серверные логи — допустимо для MVP, рекомендуется заменить на structured logging.
- **setInterval в submit-lead.ts:** Очистка rate-limit карты — работает, но в serverless-окружении может быть неэффективной. Рекомендуется внешний rate-limiter для продакшена.
- **TODO в submit-lead.ts:** «TODO: сохранение в БД (Supabase / Postgres)» — не реализовано, отмечено.

### Отсутствие критических проблем:
- Нет неиспользуемых import-ов, вызывающих ошибки сборки.
- Нет обращений к undefined/null без проверки.
- TypeScript-типизация корректна.

**Результат:** Критических ошибок не обнаружено.

---

## 7. Добавленные заголовки безопасности (next.config.ts)

В рамках аудита добавлены HTTP-заголовки:
- `X-Frame-Options: DENY` — защита от clickjacking
- `X-Content-Type-Options: nosniff` — предотвращение MIME-sniffing
- `Referrer-Policy: strict-origin-when-cross-origin` — контроль Referer
- `X-DNS-Prefetch-Control: on` — ускорение DNS-запросов
- `Permissions-Policy` — отключение камеры, микрофона, геолокации

---

## Итог

| Категория | Статус |
|---|---|
| Русский язык | Исправлены 2 проблемы. Весь контент на русском. |
| Изображения | Все пути корректны, файлы существуют. |
| Мобильная навигация | Работает корректно. |
| Формы и валидация | Надёжная клиент-серверная валидация. |
| SEO / OG-метаданные | Настроены на всех основных страницах. |
| Ошибки в коде | Критических проблем нет. |
| Заголовки безопасности | Добавлены. |
| Privacy / Terms | Заполнены, ссылки в футере присутствуют. |

**Сайт готов к запуску.** Рекомендации:
1. Добавить реальный файл `/public/og-image.jpg` (1200x630 px).
2. Заменить placeholder-телефон `+7 (XXX) XXX-XX-XX` на реальный номер.
3. Настроить Telegram-бота и добавить переменные окружения.
4. Наполнить раздел «Журнал» контентом.
